[project]
name = "sobrio-connector"
version = "0.1.0"
description = "Connecteur de facturation Sobrio — lecture seule de l'API d'administration Anthropic (Lot C)"
requires-python = ">=3.12"
dependencies = [
    "httpx",
    "sqlalchemy>=2",
    "psycopg[binary]",
]

[tool.ruff]
line-length = 100
