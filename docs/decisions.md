# Journal des décisions — Sobrio

Toute décision structurante est consignée ici (une ligne par décision).
Les évolutions de contrats passent par une RFC (`docs/rfc/`) — voir règle n°7.

| Date | Décision | Motif |
|------|----------|-------|
| 2026-07-16 | Lot 0 bootstrappé, contrats v1.0 figés (`contracts/`) | Base commune stable pour les lots A-F ; tout changement de contrat = RFC + version |
| 2026-07-16 | `uv` non disponible sur la machine de bootstrap → venv racine `.venv` + `requirements.txt` par package | Rester sur l'outillage présent, sans dépendance exotique |
| 2026-07-16 | gitleaks non installable → hook pre-commit de détection de secrets en regex simple (`language: pygrep`) | Garder un garde-fou anti-secret (règle n°5) sans binaire externe |
| 2026-07-16 | `docs/DECOUPAGE_DEV_PHASE1.md` non fourni au bootstrap → à ajouter | Le document de découpage détaillé sera versionné dès réception |
| 2026-07-16 | Taux de conversion fixe `EUR_PER_USD = 0.92` en attendant une source de taux | Débloquer les calculs de coût ; TODO : brancher une vraie source de taux |
| 2026-07-16 | Module `sobrio_impact` installé en éditable et partagé entre API et warehouse | Une seule implémentation du type `Range` et de `estimate()` (règle n°3) |
| 2026-07-16 | `usage_daily` : sentinelle `''` (chaîne vide, jamais NULL) pour `workspace_id`/`api_key_id`/`user_pseudonym` non applicables | Postgres traite les NULL comme distincts dans un index UNIQUE → `ON CONFLICT DO NOTHING` inopérant, idempotence cassée. Filtrer avec `= ''`, pas `IS NULL` |
| 2026-07-16 | CORS activé sur l'API pour l'origine `https://claude.ai` (+ localhost dev), configurable via `SOBRIO_CORS_ORIGINS` | Le content script (Lot A) appelle l'API depuis claude.ai ; sans préflight OPTIONS autorisé, l'extension dégrade silencieusement et n'affiche rien |
| 2026-07-16 | Le Makefile charge et exporte `.env` (`-include .env` + `export`) | `make sync-fixtures` exige `PSEUDONYM_SALT` (règle n°1) ; les cibles doivent voir les variables d'environnement de dev |
| 2026-07-16 | **Amendement règle n°2** (décision fondateur) : application automatique du modèle dans claude.ai en **opt-in strict** — case popup désactivée par défaut, action déclenchée uniquement par le clic « Utiliser… » de l'utilisateur, module unique `extension/src/modelSwitcher.ts`, abandon silencieux au moindre doute | Demande produit assumée en connaissance des risques (fragilité sélecteurs, CGU Anthropic, pitch « n'automatise jamais » nuancé en « n'agit que si vous l'activez ») ; gating org `allow_auto_apply` proposé en RFC-0001 |
| 2026-07-16 | **Amendement règle n°2, v2** (décision fondateur) : l'application automatique passe **activée par défaut** (désactivable dans le popup — décochée, retour à la lecture seule stricte) | Choix produit assumé ; l'action reste déclenchée uniquement par le clic « Utiliser… » de l'utilisateur et vérifiée a posteriori ; le gating org `allow_auto_apply` (RFC-0001) devient d'autant plus important pour les déploiements entreprise |
| 2026-07-17 | **Extension productisée (V1 1.0.0)** en 9 boucles autonomes ; auto-apply conservé et industrialisé (choix fondateur en réponse à la question de cadrage) | Le référentiel de productisation décrivait une règle 2 stricte, incompatible avec la décision auto-apply des 16/07 ; écart tranché en faveur du maintien, documenté dans `extension/UPGRADE_PLAN.md` |
| 2026-07-17 | Page d'entraînement supprimée → **fixtures DOM headless** (nominal/alt1/alt2/broken) ; `pnpm dev` cible claude.ai en auto-rechargement | Les fondateurs testent directement sur claude.ai ; les boucles restent auto-vérifiables sans navigateur |
| 2026-07-17 | Clé de conversation = `/chat/<id>` \| `/project/<id>` \| `new` ; registre multi-conversations plafonné à **25** (éviction LRU) | Décision raisonnable en l'absence de spécification ; garde-fou mémoire pour les longues sessions |
| 2026-07-17 | Opt-out télémétrie d'organisation lu défensivement via un champ `telemetry_enabled` (défaut = autorisé) ; auto-désactivation sur `min_extension_version` | Champs non présents au contrat v1.0 — proposés dans la RFC-0001 ; lus sans casser si absents |
| 2026-07-17 | **Catalogue de modèles mis à jour** (Chantier C, finition orchestrée) : gamme courante Haiku 4.5 / Sonnet 5 / Opus 4.8 / Fable 5, ids = identifiants d'API `claude-...` ; version catalogue `2026-07.2` ; formalisé par **RFC-0002** (règle n°7) | Gamme vérifiée EN LIGNE ; changement de contrat partagé → RFC + CHANGELOG + mise à jour de TOUS les consommateurs (extension ET lots Python : api/connector/warehouse/report). Défaut de régression inter-lots relevé par le juge `qa-auditor` en ronde 0 (j'avais lancé la seule suite extension) → correction : `make test` complet devient le critère |
| 2026-07-17 | **Claude Fable 5 non exposé à la dérogation** (`visible: false` au catalogue ; filtré du `models_visible` mock/API) | Modèle le plus cher et le plus impactant ; contraire à l'argument de sobriété. Gardé pour le chiffrage/impact, hors choix utilisateur |
| 2026-07-17 | **Prix affiché = tarif durable (standard) du Sonnet 5** (3/15), pas le prix d'introduction (2/10 actif jusqu'au 2026-08-31) | Un outil de maîtrise du coût doit refléter le coût en régime permanent ; afficher le tarif promotionnel sous-estimerait le coût réel après la fin de la promo. Le catalogue conserve `intro_price_*` à titre informatif |
| 2026-07-17 | **assist_mode (Chantier B, finition orchestrée)** : bascule de modèle encadrée pilotée par la config org — `auto` (bascule immédiate si confiance ≥ seuil 0,75 + confirmation & Annuler), `one_click` (défaut), `guide` (aucun contact page). Mode effectif = opt-in local ∧ politique org ; échec sélecteurs → repli silencieux `guide` + `selector_broken`. Formalisé par **RFC-0003**, `openapi` v1.1 (deux champs optionnels, compat ascendante) | Le pitch « n'automatise jamais » est nuancé depuis les amendements du 16/07 ; il fallait un pilotage de flotte + un **kill-switch de prudence CGU** (`guide`) sans redéploiement. Champs optionnels → configs déjà déployées inchangées (one_click, 0,75). UI optimiste + Annuler = réversibilité (règle 2) ; la bascule ne touche que le sélecteur de modèle (règle 1 : aucun texte ne part) |
